<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background color with rounded corners -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <solid android:color="#54A0FA" />
            <stroke android:width="1dp" android:color="#54A0FA"/>

        </shape>
    </item>

    <!-- Inset shadow to give the concave effect -->
    <item android:left="4dp" android:top="4dp" android:right="4dp" android:bottom="4dp">
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <solid android:color="#3994FF"/> <!-- Lighter shade for inset shadow -->
        </shape>
    </item>

</layer-list>
